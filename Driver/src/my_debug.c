
#include "my_debug.h"
#include "GlobalDef.h"

#if 0
void assert_failed(uint8_t* file, uint32_t line)
{
    os_printf("Wrong parameters value: file %s on line %d\r\n", file, line);
    while(1);
}
#endif

